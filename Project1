pipeline{
    agent {
    label "node1"
  }    
    stages{
        stage("Python Version"){
            steps{
                sh "python --version" 
                sh "pwd"
                sh "ls"
                sh "python /var/jenkins/script.py"
            }
        }
    }
}
